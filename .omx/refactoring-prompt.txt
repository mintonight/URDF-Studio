你是一个资深的 React/Three.js 重构专家。

## 任务
完成 URDF Studio 的渲染前端统一重构，将 USD 和非 USD 渲染路径统一为单一前端 RobotModel。

## 当前状态
- 部分文件已创建：
  - src/shared/components/3d/renderers/types.ts
  - src/shared/components/3d/renderers/ThreeJsBackend.ts
  - src/shared/components/3d/renderers/UsdWasmBackend.ts
  - src/features/urdf-viewer/components/RobotModelUnified.tsx

- 待删除文件仍然存在：
  - src/features/urdf-viewer/components/UsdWasmStage.tsx (~3759 行)
  - src/features/urdf-viewer/components/UsdOffscreenStage.tsx (~900 行)
  - src/features/urdf-viewer/components/UsdOriginTransformControls.tsx
  - src/features/urdf-viewer/components/UsdCollisionTransformControls.tsx

## 核心要求

### 1. 数据流必须正确维护到 robotState
- USD 格式加载后，必须解析并更新 robotStore 的 robotState
- robotState 包含：links, joints, materials, rootLinkId, name
- USD 到 robotState 的转换逻辑在 UsdWasmBackend 中实现
- 确保 useRobotLoader 能够正确读取和使用 robotState

### 2. 完成所有 8 个 Phase
- Phase 1: 已完成（后端接口）
- Phase 2: 已完成（ThreeJsBackend）
- Phase 3: 已完成（UsdWasmBackend）
- Phase 4: 需要完善（统一交互 hooks）
- Phase 5: 需要完成（统一变换控制组件）
- Phase 6: 需要完成（重构 RobotModel 为统一前端）
- Phase 7: 需要完成（简化 ViewerScene，删除格式分支）
- Phase 8: 需要完成（清理和测试）

### 3. 测试文件
必须确保以下格式的文件能正确显示：
- test/unitree_model/**/*.usd (USD 格式)
- test/unitree_ros/**/*.urdf (URDF 格式)
- test/unitree_ros_usda/**/*.usda (USDA 格式)
- test/mujoco_menagerie-main/**/*.xml (MJCF 格式)

## 具体步骤

### Step 1: 完善 Phase 4 - 统一交互 Hooks
- 确保 useRendererBackend.ts 正确管理后端生命周期
- 确保 useMouseInteraction, useHoverDetection, useHighlightManager 支持统一后端

### Step 2: 完成 Phase 5 - 统一变换控制组件
- 将 UsdOriginTransformControls 和 UsdCollisionTransformControls 的功能合并到 OriginTransformControls 和 CollisionTransformControls
- 使用 RobotRendererBackend 接口更新变换
- 删除 USD 特定的变换控制组件

### Step 3: 完成 Phase 6 - 重构 RobotModel
- 将 RobotModelUnified.tsx 的内容合并到 RobotModel.tsx
- 确保 RobotModel.tsx 完全格式无关
- 确保所有交互功能（选择、悬停、IK、绘制、变换）在所有格式下工作

### Step 4: 完成 Phase 7 - 简化 ViewerScene
- 修改 ViewerScene.tsx，移除所有格式分支逻辑
- 只保留对 RobotModel 的调用
- 删除对 UsdWasmStage 和 UsdOffscreenStage 的引用

### Step 5: 完成 Phase 8 - 清理和测试
- 删除 UsdWasmStage.tsx
- 删除 UsdOffscreenStage.tsx
- 删除 UsdOriginTransformControls.tsx
- 删除 UsdCollisionTransformControls.tsx
- 删除 RobotModelUnified.tsx（已合并）
- 删除 ViewerSceneUnified.tsx（如果存在）
- 运行 npm run typecheck 修复类型错误
- 运行 npm run lint 修复 lint 错误
- 运行 npm run test 确保测试通过

## 验收标准
1. UsdWasmStage.tsx 和 UsdOffscreenStage.tsx 被删除
2. ViewerScene.tsx 无格式分支（无 sourceFile?.format === 'usd' 判断）
3. RobotModel.tsx 是唯一的前端组件
4. 所有格式通过统一的 RobotModel 渲染
5. USD 格式加载后，robotState 中的 links, joints, materials 正确维护
6. TypeScript 类型检查通过
7. 所有测试通过

## 关键约束
- 不要只做部分工作，必须完成所有 Phase
- 必须确保数据流正确维护到 robotState
- 完成后报告所有修改的文件和删除的文件

开始执行，完成后报告结果。
