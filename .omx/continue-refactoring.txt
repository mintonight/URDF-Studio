继续完成 URDF Studio 渲染前端统一重构。

## 已完成
- Phase 1: 后端接口 (src/shared/components/3d/renderers/types.ts)
- Phase 2: ThreeJsBackend (src/shared/components/3d/renderers/ThreeJsBackend.ts)
- Phase 3: UsdWasmBackend 部分完成 (src/shared/components/3d/renderers/UsdWasmBackend.ts)

## 待完成

### Phase 4: 完善 UsdWasmBackend
当前 UsdWasmBackend.ts 存在但可能不完整。需要确保：
1. 实现完整的 RobotRendererBackend 接口
2. 确保 USD 加载后能正确解析 robotState
3. 确保 raycast 功能正常
4. 确保更新变换功能正常

### Phase 5: 统一变换控制组件
1. 将 UsdOriginTransformControls 和 UsdCollisionTransformControls 的功能合并
2. 修改 OriginTransformControls 和 CollisionTransformControls 支持后端
3. 删除 USD 特定的变换控制组件

### Phase 6: 重构 RobotModel
1. 将 RobotModelUnified.tsx 的内容合并到 RobotModel.tsx
2. 确保 RobotModel.tsx 根据格式选择后端
3. 确保所有交互功能在所有格式下工作

### Phase 7: 简化 ViewerScene
1. 修改 ViewerScene.tsx，移除所有格式分支
2. 只保留 RobotModel 调用
3. 删除 UsdWasmStage 和 UsdOffscreenStage 引用

### Phase 8: 清理
1. 删除以下文件：
   - src/features/urdf-viewer/components/UsdWasmStage.tsx
   - src/features/urdf-viewer/components/UsdOffscreenStage.tsx
   - src/features/urdf-viewer/components/UsdOriginTransformControls.tsx
   - src/features/urdf-viewer/components/UsdCollisionTransformControls.tsx
   - src/features/urdf-viewer/components/RobotModelUnified.tsx
   - src/features/urdf-viewer/components/ViewerSceneUnified.tsx

2. 修复所有导入引用

3. 运行 npm run typecheck

4. 运行 npm run lint

5. 运行 npm run test

## 关键要求
- 必须确保 USD 格式加载后，robotState 中的 links, joints, materials 正确维护
- 必须删除所有 USD 特定的前端组件
- ViewerScene 不能有任何格式分支逻辑

开始执行，完成后报告所有修改和删除的文件。
